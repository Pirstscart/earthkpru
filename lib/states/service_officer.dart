import 'package:flutter/material.dart';

class ServiceOfficer extends StatelessWidget {
  const ServiceOfficer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}